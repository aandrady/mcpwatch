"""Analyses computed from the corpus.

Everything here is derived and disposable: each module reads the append-only
corpus and writes nothing back to it, so a report can always be regenerated and
never has to be trusted as a source. The corpus is the record; these are
readings of it.
"""
